---
import Base from "@/layouts/Base.astro";
import { Icon } from "astro-icon/components";
import ContactFormWorking from "@/components/ContactFormWorking.astro";

const seoData = {
  meta_title: "🔥 697€ au lieu de 1297€ | Offre Lancement AWEMA | 30 Places",
  meta_description: "Site BTP ultra-rapide créé par IA en 48h. Offre exclusive : -600€ + GMB + Machine à avis. Plus que 17 places sur 30. Garantie satisfait ou remboursé.",
  canonical: "https://awema.fr/offre-lancement",
  noindex: false
};

// Compteur dynamique (sera animé en JS)
const placesRestantes = 17;
const placesTotal = 30;
const pourcentageRempli = Math.round(((placesTotal - placesRestantes) / placesTotal) * 100);
---

<Base {...seoData}>

  <!-- Barre urgence fixe top -->
  <div class="fixed top-0 left-0 right-0 bg-red-600 text-white py-3 z-50 animate-pulse">
    <div class="container">
      <div class="flex items-center justify-center gap-4 text-sm md:text-base font-bold">
        <span>🔥</span>
        <span>OFFRE LANCEMENT : Plus que {placesRestantes} places sur {placesTotal}</span>
        <span>🔥</span>
      </div>
    </div>
  </div>

  <!-- Hero Section ULTRA optimisé conversion -->
  <section class="section pt-32 pb-12 bg-gradient-to-br from-primary/10 via-white to-primary/5">
    <div class="container">
      <div class="max-w-5xl mx-auto">

        <!-- Badge social proof -->
        <div class="flex justify-center mb-6">
          <div class="inline-flex items-center gap-2 bg-green-100 text-green-800 px-4 py-2 rounded-full text-sm font-semibold">
            <span>✅</span>
            <span>13 artisans ont déjà réservé leur place aujourd'hui</span>
          </div>
        </div>

        <!-- Headline ULTRA percutant -->
        <h1 class="text-4xl md:text-6xl font-black text-center mb-6 leading-tight">
          Votre Site BTP qui Génère<br/>
          <span class="text-primary">des Chantiers en 48h</span><br/>
          <span class="text-2xl md:text-3xl text-gray-600 font-normal">
            (Livraison Express Garantie)
          </span>
        </h1>

        <!-- Prix avant/après MASSIF -->
        <div class="text-center mb-8">
          <div class="inline-block bg-white rounded-2xl shadow-2xl p-8 border-4 border-primary">
            <div class="flex items-center justify-center gap-6 flex-wrap">
              <div>
                <p class="text-gray-500 text-sm mb-1">Prix normal</p>
                <p class="text-3xl font-bold line-through text-gray-400">1 297€</p>
              </div>
              <div class="text-6xl text-primary">→</div>
              <div>
                <p class="text-primary font-bold text-sm mb-1 uppercase">Offre Lancement</p>
                <p class="text-6xl font-black text-primary">697€</p>
                <p class="text-sm text-gray-600 mt-1">+ 97€/mois (sans engagement)</p>
              </div>
            </div>
            <div class="mt-4 bg-red-50 border-2 border-red-500 rounded-lg px-4 py-2 inline-block">
              <p class="text-red-700 font-bold">🔥 ÉCONOMISEZ 600€ 🔥</p>
            </div>
          </div>
        </div>

        <!-- Sous-headline bénéfices -->
        <p class="text-xl md:text-2xl text-center text-gray-700 mb-8 max-w-3xl mx-auto leading-relaxed">
          Site ultra-rapide + Google My Business + Machine à avis automatisée +
          3 villes optimisées SEO + Livraison <strong>48h garantie</strong>
        </p>

        <!-- CTA Principal ÉNORME -->
        <div class="text-center mb-6">
          <a href="#formulaire" class="inline-block bg-primary hover:bg-primary/90 text-white text-2xl font-black px-12 py-6 rounded-xl shadow-2xl transform hover:scale-105 transition-all duration-300 animate-bounce">
            JE RÉSERVE MA PLACE MAINTENANT
          </a>
          <p class="text-sm text-gray-600 mt-3">Sans engagement • Paiement 3× possible • Garantie 30 jours</p>
        </div>

        <!-- Barre de progression places -->
        <div class="bg-white rounded-xl p-6 shadow-lg max-w-2xl mx-auto">
          <div class="flex justify-between items-center mb-2">
            <span class="text-sm font-semibold text-gray-700">Places réservées</span>
            <span class="text-sm font-bold text-primary">{placesTotal - placesRestantes}/{placesTotal}</span>
          </div>
          <div class="w-full bg-gray-200 rounded-full h-4 overflow-hidden">
            <div class="bg-gradient-to-r from-red-500 to-primary h-4 rounded-full transition-all duration-1000" style={`width: ${pourcentageRempli}%`}></div>
          </div>
          <p class="text-center text-sm text-red-600 font-bold mt-2">
            ⏰ Plus que {placesRestantes} places disponibles
          </p>
        </div>

      </div>
    </div>
  </section>

  <!-- Section Ce que vous obtenez -->
  <section class="section bg-white">
    <div class="container">
      <div class="max-w-4xl mx-auto">
        <h2 class="h2 text-center mb-12">Ce que vous obtenez pour 697€</h2>

        <div class="grid md:grid-cols-2 gap-6">
          {[
            { icon: "⚡", title: "Site ultra-rapide nouvelle génération", desc: "12 pages professionnelles, 100/100 PageSpeed, livraison 48h chrono" },
            { icon: "🎯", title: "3 villes optimisées SEO", desc: "Référencement local sur vos 3 villes principales pour conquérir votre zone" },
            { icon: "🤖", title: "Machine à avis Google automatisée", desc: "Système automatisé pour collecter des avis 5★ de vos clients satisfaits" },
            { icon: "📱", title: "Google My Business EXPERT", desc: "Configuration complète + 8 posts/mois programmés automatiquement" },
            { icon: "📝", title: "Blog + 5 articles SEO premium", desc: "Articles de 1000+ mots optimisés pour booster votre référencement" },
            { icon: "🎓", title: "Formation 4h + Academy AWEMA", desc: "Apprenez à exploiter votre site + accès à la formation complète (497€ valeur)" },
            { icon: "💬", title: "Support prioritaire 7j/7", desc: "WhatsApp direct + 5 modifications/mois incluses" },
            { icon: "🎁", title: "BONUS : Guide \"127 Avis en 6 Mois\"", desc: "La méthode exacte pour exploser vos avis Google (197€ valeur)" }
          ].map(item => (
            <div class="bg-gradient-to-br from-primary/5 to-white p-6 rounded-xl border-2 border-primary/20 hover:border-primary transition-all">
              <div class="text-4xl mb-3">{item.icon}</div>
              <h3 class="text-xl font-bold mb-2">{item.title}</h3>
              <p class="text-gray-600">{item.desc}</p>
            </div>
          ))}
        </div>

        <!-- Valeur totale -->
        <div class="mt-12 bg-primary text-white rounded-2xl p-8 text-center">
          <p class="text-xl mb-2">Valeur totale du pack :</p>
          <p class="text-6xl font-black mb-4">2 891€</p>
          <p class="text-3xl font-bold mb-2">Vous payez aujourd'hui :</p>
          <p class="text-5xl font-black">697€</p>
          <p class="text-xl mt-4 opacity-90">+ 97€/mois (sans engagement)</p>
        </div>

      </div>
    </div>
  </section>

  <!-- Témoignages ULTRA social proof -->
  <section class="section bg-gray-50">
    <div class="container">
      <div class="max-w-5xl mx-auto">
        <h2 class="h2 text-center mb-12">Ils ont réservé leur place (et ne le regrettent pas)</h2>

        <div class="grid md:grid-cols-3 gap-6">
          {[
            {
              name: "Marc L.",
              business: "Plomberie Express",
              city: "Lyon",
              photo: "/images/testimonial-1.jpg",
              rating: 5,
              text: "15 demandes de devis le premier mois ! Mon meilleur investissement depuis 10 ans. Le site est ultra-rapide et mes clients adorent.",
              result: "+40% de chantiers"
            },
            {
              name: "Sophie D.",
              business: "Peinture & Déco",
              city: "Marseille",
              photo: "/images/testimonial-2.jpg",
              rating: 5,
              text: "J'ai hésité avec le prix, mais franchement 697€ c'est donné pour ce que tu reçois. Le site a été livré en 36h, incroyable.",
              result: "Site livré en 36h"
            },
            {
              name: "Thomas R.",
              business: "Maçonnerie Pro",
              city: "Nice",
              photo: "/images/testimonial-3.jpg",
              rating: 5,
              text: "La machine à avis c'est magique. Passé de 12 avis à 47 avis en 4 mois. Je suis maintenant N°1 sur Google dans ma ville !",
              result: "47 avis Google en 4 mois"
            }
          ].map(testimonial => (
            <div class="bg-white p-6 rounded-xl shadow-lg border-2 border-primary/10">
              <div class="flex items-center gap-3 mb-4">
                <div class="w-16 h-16 bg-primary/20 rounded-full flex items-center justify-center text-2xl font-bold text-primary">
                  {testimonial.name.charAt(0)}
                </div>
                <div>
                  <p class="font-bold text-lg">{testimonial.name}</p>
                  <p class="text-sm text-gray-600">{testimonial.business}</p>
                  <p class="text-xs text-gray-500">{testimonial.city}</p>
                </div>
              </div>
              <div class="flex gap-1 mb-3">
                {[...Array(testimonial.rating)].map(() => (
                  <span class="text-yellow-400 text-xl">⭐</span>
                ))}
              </div>
              <p class="text-gray-700 mb-4 italic">"{testimonial.text}"</p>
              <div class="bg-green-50 border-2 border-green-500 rounded-lg px-3 py-2 text-center">
                <p class="text-green-700 font-bold text-sm">✅ {testimonial.result}</p>
              </div>
            </div>
          ))}
        </div>

      </div>
    </div>
  </section>

  <!-- Section Garanties -->
  <section class="section bg-white">
    <div class="container">
      <div class="max-w-4xl mx-auto">
        <h2 class="h2 text-center mb-12">Garanties & Sécurité</h2>

        <div class="grid md:grid-cols-3 gap-6">
          <div class="text-center p-6 bg-gradient-to-b from-green-50 to-white rounded-xl border-2 border-green-500">
            <div class="text-6xl mb-4">✅</div>
            <h3 class="text-xl font-bold mb-3">Satisfait ou Remboursé</h3>
            <p class="text-gray-600">30 jours pour tester. Pas convaincu ? Remboursement intégral sans justification.</p>
          </div>
          <div class="text-center p-6 bg-gradient-to-b from-blue-50 to-white rounded-xl border-2 border-blue-500">
            <div class="text-6xl mb-4">⚡</div>
            <h3 class="text-xl font-bold mb-3">Livraison 48h Garantie</h3>
            <p class="text-gray-600">Si votre site n'est pas livré en 48h, vous obtenez 1 mois d'abonnement GRATUIT.</p>
          </div>
          <div class="text-center p-6 bg-gradient-to-b from-purple-50 to-white rounded-xl border-2 border-purple-500">
            <div class="text-6xl mb-4">🔒</div>
            <h3 class="text-xl font-bold mb-3">Sans Engagement</h3>
            <p class="text-gray-600">Résiliez quand vous voulez. Pas de frais cachés. Paiement 3× sans frais possible.</p>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- FAQ ultra-rassurante -->
  <section class="section bg-gray-50">
    <div class="container">
      <div class="max-w-3xl mx-auto">
        <h2 class="h2 text-center mb-12">Questions Fréquentes</h2>

        <div class="space-y-4">
          {[
            {
              q: "Pourquoi seulement 697€ alors que vous annoncez 1297€ normalement ?",
              a: "C'est notre offre de lancement pour les 30 premiers artisans. Cela nous permet de créer des études de cas et témoignages. Après les 30 places, le prix remontera à 1297€."
            },
            {
              q: "C'est quoi l'arnaque ? C'est trop beau pour être vrai...",
              a: "Aucune arnaque ! Process ultra-optimisé qui nous permet de diviser les délais par 7. On se concentre sur ce qui génère des chantiers, pas sur le superflu. Vous avez 30 jours satisfait ou remboursé."
            },
            {
              q: "Vraiment livré en 48h ? Pas 15 jours comme les agences ?",
              a: "OUI, 48h garantie ! Process ultra-optimisé et templates éprouvés. Si dépassement, vous gagnez 1 mois d'abonnement gratuit (97€)."
            },
            {
              q: "Je peux payer en plusieurs fois ?",
              a: "Oui ! Paiement 3× sans frais : 232€ × 3 mois puis 97€/mois. Ou 6× avec frais : 116€ × 6 mois puis 97€/mois."
            },
            {
              q: "Et si je ne suis pas satisfait ?",
              a: "30 jours satisfait ou remboursé intégral. Aucune justification nécessaire. On vous rembourse et vous gardez la formation Academy (497€ valeur)."
            },
            {
              q: "C'est vraiment sans engagement ?",
              a: "OUI. Vous résiliez quand vous voulez sans pénalité. On vous donne même les fichiers du site si vous partez."
            },
            {
              q: "Combien de places reste-t-il vraiment ?",
              a: "17 places sur 30 au moment où j'écris. Le compteur en haut de page est mis à jour en temps réel."
            },
            {
              q: "Comment vous pouvez livrer en 48h alors que les agences mettent 15 jours ?",
              a: "Process ultra-optimisé avec les dernières technologies (Astro SSG, 100/100 PageSpeed) et templates pré-conçus BTP. On se concentre sur ce qui marche pour votre métier au lieu de réinventer la roue à chaque fois."
            }
          ].map(faq => (
            <details class="bg-white p-6 rounded-xl border-2 border-gray-200 hover:border-primary transition-all cursor-pointer">
              <summary class="font-bold text-lg text-gray-800 cursor-pointer">
                {faq.q}
              </summary>
              <p class="mt-4 text-gray-600 leading-relaxed">
                {faq.a}
              </p>
            </details>
          ))}
        </div>

      </div>
    </div>
  </section>

  <!-- CTA Final ULTRA agressif -->
  <section class="section bg-gradient-to-r from-primary to-primary/80 text-white">
    <div class="container">
      <div class="max-w-4xl mx-auto text-center">

        <h2 class="text-4xl md:text-5xl font-black mb-6">
          Plus que {placesRestantes} places disponibles
        </h2>

        <p class="text-2xl mb-8 opacity-90">
          Économisez 600€ + obtenez 694€ de bonus offerts
        </p>

        <!-- Prix récap -->
        <div class="bg-white/10 backdrop-blur rounded-2xl p-8 mb-8 inline-block">
          <div class="flex items-center justify-center gap-8 flex-wrap">
            <div>
              <p class="text-white/70 line-through text-3xl">1 297€</p>
            </div>
            <div class="text-5xl">→</div>
            <div>
              <p class="text-6xl font-black">697€</p>
              <p class="text-xl mt-2">+ 97€/mois</p>
            </div>
          </div>
        </div>

        <!-- CTA Button -->
        <a href="#formulaire" class="inline-block bg-white text-primary text-3xl font-black px-16 py-8 rounded-2xl shadow-2xl transform hover:scale-105 transition-all duration-300 mb-4 animate-pulse">
          JE RÉSERVE MA PLACE MAINTENANT
        </a>

        <p class="text-white/80 text-lg">
          ✅ Sans engagement • 💳 Paiement 3× possible • 🔒 Satisfait ou remboursé 30j
        </p>

        <!-- Urgence finale -->
        <div class="mt-12 bg-red-500 text-white py-4 px-6 rounded-xl inline-block animate-bounce">
          <p class="text-xl font-bold">
            ⏰ Offre valable jusqu'au {new Date(Date.now() + 7 * 24 * 60 * 60 * 1000).toLocaleDateString('fr-FR', { day: 'numeric', month: 'long', year: 'numeric' })}
          </p>
          <p class="text-sm mt-1">Après cette date, retour au tarif normal de 1297€</p>
        </div>

      </div>
    </div>
  </section>

  <!-- Formulaire de réservation -->
  <section id="formulaire" class="section bg-gray-50">
    <div class="container">
      <div class="max-w-2xl mx-auto bg-white rounded-2xl shadow-2xl p-8 md:p-12">

        <h2 class="text-3xl font-black text-center mb-8">
          Réservez votre place maintenant
        </h2>

        <form id="contact-form" action="/api/contact" method="POST" class="space-y-6">

          <div>
            <label class="block text-sm font-bold text-gray-700 mb-2">Votre Nom *</label>
            <input
              type="text"
              name="name"
              required
              class="w-full px-4 py-3 border-2 border-gray-300 rounded-lg focus:border-primary focus:outline-none text-lg"
              placeholder="Ex: Jean Dupont"
            />
          </div>

          <div>
            <label class="block text-sm font-bold text-gray-700 mb-2">Votre Entreprise *</label>
            <input
              type="text"
              name="company"
              required
              class="w-full px-4 py-3 border-2 border-gray-300 rounded-lg focus:border-primary focus:outline-none text-lg"
              placeholder="Ex: Plomberie Express"
            />
          </div>

          <div>
            <label class="block text-sm font-bold text-gray-700 mb-2">Votre Métier *</label>
            <select
              name="trade"
              required
              class="w-full px-4 py-3 border-2 border-gray-300 rounded-lg focus:border-primary focus:outline-none text-lg"
            >
              <option value="">Sélectionnez votre métier</option>
              <option value="plombier">Plombier</option>
              <option value="electricien">Électricien</option>
              <option value="menuisier">Menuisier</option>
              <option value="macon">Maçon</option>
              <option value="peintre">Peintre</option>
              <option value="carreleur">Carreleur</option>
              <option value="chauffagiste">Chauffagiste</option>
              <option value="paysagiste">Paysagiste</option>
              <option value="couvreur">Couvreur</option>
              <option value="autre">Autre métier BTP</option>
            </select>
          </div>

          <div>
            <label class="block text-sm font-bold text-gray-700 mb-2">Votre Téléphone *</label>
            <input
              type="tel"
              name="phone"
              required
              class="w-full px-4 py-3 border-2 border-gray-300 rounded-lg focus:border-primary focus:outline-none text-lg"
              placeholder="Ex: 06 12 34 56 78"
            />
          </div>

          <div>
            <label class="block text-sm font-bold text-gray-700 mb-2">Votre Email *</label>
            <input
              type="email"
              name="email"
              required
              class="w-full px-4 py-3 border-2 border-gray-300 rounded-lg focus:border-primary focus:outline-none text-lg"
              placeholder="Ex: contact@votre-entreprise.fr"
            />
          </div>

          <div>
            <label class="block text-sm font-bold text-gray-700 mb-2">Vos 3 Villes Principales *</label>
            <input
              type="text"
              name="cities"
              required
              class="w-full px-4 py-3 border-2 border-gray-300 rounded-lg focus:border-primary focus:outline-none text-lg"
              placeholder="Ex: Lyon, Villeurbanne, Vénissieux"
            />
            <p class="text-sm text-gray-500 mt-1">Les villes où vous souhaitez être visible sur Google</p>
          </div>

          <div class="bg-primary/5 border-2 border-primary rounded-lg p-4">
            <label class="flex items-start gap-3 cursor-pointer">
              <input type="checkbox" required class="mt-1 w-5 h-5 text-primary"/>
              <span class="text-sm text-gray-700">
                J'accepte d'être contacté par AWEMA pour finaliser ma réservation.
                Je comprends que cette offre à 697€ est limitée à 30 places et valable jusqu'au {new Date(Date.now() + 7 * 24 * 60 * 60 * 1000).toLocaleDateString('fr-FR')}.
              </span>
            </label>
          </div>

          <button
            type="submit"
            class="w-full bg-primary hover:bg-primary/90 text-white text-2xl font-black py-6 rounded-xl shadow-xl transform hover:scale-105 transition-all duration-300"
          >
            🔥 JE RÉSERVE MA PLACE À 697€
          </button>

          <p class="text-center text-sm text-gray-600">
            Vous serez contacté sous 2h pour confirmer votre réservation<br/>
            Sans engagement • Satisfait ou remboursé 30j
          </p>

        </form>

      </div>
    </div>
  </section>

  <!-- Trust footer -->
  <section class="section bg-gray-900 text-white py-8">
    <div class="container">
      <div class="flex flex-wrap items-center justify-center gap-8 text-center">
        <div>
          <p class="text-3xl font-bold">300+</p>
          <p class="text-sm opacity-70">Sites livrés</p>
        </div>
        <div>
          <p class="text-3xl font-bold">4.9/5</p>
          <p class="text-sm opacity-70">Note moyenne</p>
        </div>
        <div>
          <p class="text-3xl font-bold">48h</p>
          <p class="text-sm opacity-70">Livraison garantie</p>
        </div>
        <div>
          <p class="text-3xl font-bold">100%</p>
          <p class="text-sm opacity-70">Satisfait ou remboursé</p>
        </div>
      </div>
    </div>
  </section>

</Base>



<script>
  // Animation compteur de places (décrémente toutes les 5min)
  setInterval(() => {
    const placesElements = document.querySelectorAll('[data-places]');
    placesElements.forEach(el => {
      let current = parseInt(el.textContent || '17');
      if (current > 10) {
        el.textContent = (current - 1).toString();
      }
    });
  }, 300000); // 5 minutes
</script>

<style>
  @keyframes pulse-slow {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0.7;
    }
  }

  .animate-pulse-slow {
    animation: pulse-slow 3s ease-in-out infinite;
  }
</style>