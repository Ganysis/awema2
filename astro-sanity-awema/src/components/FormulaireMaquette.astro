---
export interface Props {
  title?: string;
  subtitle?: string;
  urgencyMessage?: string;
  buttonText?: string;
  formId?: string;
}

const {
  title = "Recevez vos 3 maquettes gratuites en 24h",
  subtitle = "Découvrez à quoi ressemblera votre futur site professionnel",
  urgencyMessage = "🔥 Plus que 2 places disponibles ce mois-ci",
  buttonText = "Recevoir mes maquettes gratuites",
  formId = "maquette-form"
} = Astro.props;
---

<div class="formulaire-maquette-wrapper" data-aos="fade-up">
  <div class="bg-gradient-to-br from-primary/5 to-white rounded-2xl shadow-2xl p-8 lg:p-12 relative overflow-hidden">
    <!-- Background decoration -->
    <div class="absolute top-0 right-0 w-64 h-64 bg-primary/10 rounded-full blur-3xl -mr-32 -mt-32"></div>
    <div class="absolute bottom-0 left-0 w-64 h-64 bg-secondary/10 rounded-full blur-3xl -ml-32 -mb-32"></div>

    <!-- Urgency badge -->
    {urgencyMessage && (
      <div class="absolute top-4 right-4 bg-red-500 text-white px-4 py-2 rounded-full text-sm font-bold animate-pulse">
        {urgencyMessage}
      </div>
    )}

    <!-- Form content -->
    <div class="relative z-10">
      <div class="text-center mb-8">
        <h2 class="text-3xl lg:text-4xl font-bold mb-4 text-gray-900">
          {title}
        </h2>
        <p class="text-lg text-gray-600 mb-4">
          {subtitle}
        </p>

        <!-- Value props -->
        <div class="flex flex-wrap justify-center gap-4 mb-6">
          <span class="inline-flex items-center text-sm">
            <svg class="w-5 h-5 text-green-500 mr-1" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
            Sans engagement
          </span>
          <span class="inline-flex items-center text-sm">
            <svg class="w-5 h-5 text-green-500 mr-1" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
            100% gratuit
          </span>
          <span class="inline-flex items-center text-sm">
            <svg class="w-5 h-5 text-green-500 mr-1" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
            Livraison 24h
          </span>
        </div>
      </div>

      <!-- Form -->
      <form id={formId} class="space-y-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <!-- Nom -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Votre nom *
            </label>
            <div class="relative">
              <input
                type="text"
                name="name"
                required
                class="w-full px-4 py-3 pl-10 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
                placeholder="Jean Dupont"
              />
              <svg class="absolute left-3 top-3.5 w-4 h-4 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd"/>
              </svg>
            </div>
          </div>

          <!-- Entreprise -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Votre entreprise *
            </label>
            <div class="relative">
              <input
                type="text"
                name="company"
                required
                class="w-full px-4 py-3 pl-10 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
                placeholder="Plomberie Dupont"
              />
              <svg class="absolute left-3 top-3.5 w-4 h-4 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M4 4a2 2 0 012-2h8a2 2 0 012 2v12a1 1 0 110 2h-3a1 1 0 01-1-1v-2a1 1 0 00-1-1H9a1 1 0 00-1 1v2a1 1 0 01-1 1H4a1 1 0 110-2V4z" clip-rule="evenodd"/>
              </svg>
            </div>
          </div>

          <!-- Email -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Email professionnel *
            </label>
            <div class="relative">
              <input
                type="email"
                name="email"
                required
                class="w-full px-4 py-3 pl-10 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
                placeholder="contact@exemple.fr"
              />
              <svg class="absolute left-3 top-3.5 w-4 h-4 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"/>
                <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"/>
              </svg>
            </div>
          </div>

          <!-- Téléphone -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Téléphone *
            </label>
            <div class="relative">
              <input
                type="tel"
                name="phone"
                required
                pattern="[0-9]{10}"
                class="w-full px-4 py-3 pl-10 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
                placeholder="0612345678"
              />
              <svg class="absolute left-3 top-3.5 w-4 h-4 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                <path d="M2 3a1 1 0 011-1h2.153a1 1 0 01.986.836l.74 4.435a1 1 0 01-.54 1.06l-1.548.773a11.037 11.037 0 006.105 6.105l.774-1.548a1 1 0 011.059-.54l4.435.74a1 1 0 01.836.986V17a1 1 0 01-1 1h-2C7.82 18 2 12.18 2 5V3z"/>
              </svg>
            </div>
          </div>

          <!-- Métier -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Votre métier *
            </label>
            <div class="relative">
              <select
                name="metier"
                required
                class="w-full px-4 py-3 pl-10 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-transparent transition-all appearance-none"
              >
                <option value="">Sélectionnez votre métier</option>
                <option value="plombier">Plombier</option>
                <option value="electricien">Électricien</option>
                <option value="menuisier">Menuisier</option>
                <option value="macon">Maçon</option>
                <option value="paysagiste">Paysagiste</option>
                <option value="carreleur">Carreleur</option>
                <option value="peintre">Peintre</option>
                <option value="chauffagiste">Chauffagiste</option>
                <option value="couvreur">Couvreur</option>
                <option value="serrurier">Serrurier</option>
                <option value="autre">Autre métier du BTP</option>
              </select>
              <svg class="absolute left-3 top-3.5 w-4 h-4 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M6 6V5a3 3 0 013-3h2a3 3 0 013 3v1h2a2 2 0 012 2v3.57A22.952 22.952 0 0110 13a22.95 22.95 0 01-8-1.43V8a2 2 0 012-2h2zm2-1a1 1 0 011-1h2a1 1 0 011 1v1H8V5zm1 5a1 1 0 011-1h.01a1 1 0 110 2H10a1 1 0 01-1-1z" clip-rule="evenodd"/>
                <path d="M2 13.692V16a2 2 0 002 2h12a2 2 0 002-2v-2.308A24.974 24.974 0 0110 15c-2.796 0-5.487-.46-8-1.308z"/>
              </svg>
              <svg class="absolute right-3 top-3.5 w-4 h-4 text-gray-400 pointer-events-none" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
              </svg>
            </div>
          </div>

          <!-- Ville -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Votre ville *
            </label>
            <div class="relative">
              <input
                type="text"
                name="city"
                required
                class="w-full px-4 py-3 pl-10 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
                placeholder="Lyon"
              />
              <svg class="absolute left-3 top-3.5 w-4 h-4 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd"/>
              </svg>
            </div>
          </div>
        </div>

        <!-- Budget (optionnel) -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">
            Votre budget mensuel (optionnel)
          </label>
          <div class="grid grid-cols-2 md:grid-cols-4 gap-3">
            <label class="relative">
              <input type="radio" name="budget" value="0-100" class="sr-only peer" />
              <div class="px-4 py-2 border-2 border-gray-200 rounded-lg cursor-pointer peer-checked:border-primary peer-checked:bg-primary/5 hover:bg-gray-50 transition-all text-center">
                <span class="text-sm">< 100€</span>
              </div>
            </label>
            <label class="relative">
              <input type="radio" name="budget" value="100-200" class="sr-only peer" />
              <div class="px-4 py-2 border-2 border-gray-200 rounded-lg cursor-pointer peer-checked:border-primary peer-checked:bg-primary/5 hover:bg-gray-50 transition-all text-center">
                <span class="text-sm">100-200€</span>
              </div>
            </label>
            <label class="relative">
              <input type="radio" name="budget" value="200-300" class="sr-only peer" />
              <div class="px-4 py-2 border-2 border-gray-200 rounded-lg cursor-pointer peer-checked:border-primary peer-checked:bg-primary/5 hover:bg-gray-50 transition-all text-center">
                <span class="text-sm">200-300€</span>
              </div>
            </label>
            <label class="relative">
              <input type="radio" name="budget" value="300+" class="sr-only peer" />
              <div class="px-4 py-2 border-2 border-gray-200 rounded-lg cursor-pointer peer-checked:border-primary peer-checked:bg-primary/5 hover:bg-gray-50 transition-all text-center">
                <span class="text-sm">> 300€</span>
              </div>
            </label>
          </div>
        </div>

        <!-- Message (optionnel) -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">
            Votre projet en quelques mots (optionnel)
          </label>
          <textarea
            name="message"
            rows="3"
            class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
            placeholder="Décrivez brièvement votre projet de site web..."
          ></textarea>
        </div>

        <!-- Consentement -->
        <div class="flex items-start">
          <input
            type="checkbox"
            name="consent"
            required
            class="mt-1 mr-3 rounded border-gray-300 text-primary focus:ring-primary"
          />
          <label class="text-sm text-gray-600">
            J'accepte de recevoir mes 3 maquettes gratuites par email et d'être contacté par AWEMA pour discuter de mon projet. Je peux me désinscrire à tout moment.
          </label>
        </div>

        <!-- Submit button -->
        <button
          type="submit"
          class="w-full btn btn-primary btn-xl py-4 text-lg font-bold shadow-lg hover:shadow-xl transform hover:-translate-y-1 transition-all group"
        >
          <span>{buttonText}</span>
          <svg class="inline-block ml-2 w-5 h-5 group-hover:translate-x-1 transition-transform" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd"/>
          </svg>
        </button>

        <!-- Trust elements -->
        <div class="mt-6 flex flex-wrap items-center justify-center gap-6 text-sm text-gray-500">
          <span class="flex items-center">
            <svg class="w-4 h-4 mr-1 text-green-500" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M2.166 4.999A11.954 11.954 0 0010 1.944 11.954 11.954 0 0017.834 5c.11.65.166 1.32.166 2.001 0 5.225-3.34 9.67-8 11.317C5.34 16.67 2 12.225 2 7c0-.682.057-1.35.166-2.001zm11.541 3.708a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
            Données sécurisées
          </span>
          <span class="flex items-center">
            <svg class="w-4 h-4 mr-1 text-yellow-500" fill="currentColor" viewBox="0 0 20 20">
              <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
            </svg>
            4.9/5 sur 300+ avis
          </span>
          <span class="flex items-center">
            <svg class="w-4 h-4 mr-1 text-blue-500" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
            </svg>
            Réponse en 24h
          </span>
        </div>
      </form>
    </div>
  </div>
</div>

<script>
  // Form validation and submission
  document.addEventListener('DOMContentLoaded', () => {
    const forms = document.querySelectorAll('form[id^="maquette-form"]');

    forms.forEach(form => {
      form.addEventListener('submit', async (e) => {
        e.preventDefault();

        const formData = new FormData(form);
        const data = Object.fromEntries(formData);

        // Show loading state
        const button = form.querySelector('button[type="submit"]');
        const originalText = button.innerHTML;
        button.disabled = true;
        button.innerHTML = '<span>Envoi en cours...</span>';

        try {
          // Here you would send the data to your backend
          console.log('Form data:', data);

          // Simulate API call
          await new Promise(resolve => setTimeout(resolve, 1500));

          // Show success message
          button.innerHTML = '<span>✅ Demande envoyée !</span>';
          form.reset();

          // Show success notification
          const notification = document.createElement('div');
          notification.className = 'fixed bottom-4 right-4 bg-green-500 text-white px-6 py-4 rounded-lg shadow-lg z-50';
          notification.innerHTML = `
            <div class="flex items-center">
              <svg class="w-6 h-6 mr-3" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
              </svg>
              <div>
                <p class="font-bold">Demande reçue !</p>
                <p class="text-sm">Vous recevrez vos maquettes dans les 24h</p>
              </div>
            </div>
          `;
          document.body.appendChild(notification);

          setTimeout(() => {
            notification.remove();
            button.disabled = false;
            button.innerHTML = originalText;
          }, 5000);

        } catch (error) {
          console.error('Error:', error);
          button.innerHTML = '<span>❌ Erreur, réessayez</span>';
          setTimeout(() => {
            button.disabled = false;
            button.innerHTML = originalText;
          }, 3000);
        }
      });
    });

    // Phone number formatting
    const phoneInputs = document.querySelectorAll('input[type="tel"]');
    phoneInputs.forEach(input => {
      input.addEventListener('input', (e) => {
        let value = e.target.value.replace(/\D/g, '');
        if (value.length > 0) {
          if (value.length <= 2) {
            value = value;
          } else if (value.length <= 4) {
            value = value.slice(0, 2) + ' ' + value.slice(2);
          } else if (value.length <= 6) {
            value = value.slice(0, 2) + ' ' + value.slice(2, 4) + ' ' + value.slice(4);
          } else if (value.length <= 8) {
            value = value.slice(0, 2) + ' ' + value.slice(2, 4) + ' ' + value.slice(4, 6) + ' ' + value.slice(6);
          } else {
            value = value.slice(0, 2) + ' ' + value.slice(2, 4) + ' ' + value.slice(4, 6) + ' ' + value.slice(6, 8) + ' ' + value.slice(8, 10);
          }
        }
        e.target.value = value;
      });
    });
  });
</script>

<style>
  .formulaire-maquette-wrapper {
    margin-top: 3rem;
    margin-bottom: 3rem;
  }

  /* Custom checkbox style */
  input[type="checkbox"] {
    width: 1rem;
    height: 1rem;
    color: var(--color-primary);
    background-color: white;
    border-color: rgb(209, 213, 219);
    border-radius: 0.25rem;
  }

  input[type="checkbox"]:focus {
    --tw-ring-color: var(--color-primary);
  }

  /* Focus styles for inputs */
  input:focus, select:focus, textarea:focus {
    outline: none;
    box-shadow: 0 0 0 3px rgba(var(--color-primary-rgb), 0.2);
  }

  /* Hover effect on submit button */
  button[type="submit"]:hover {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
  }
</style>